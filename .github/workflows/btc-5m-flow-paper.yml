name: BTC 5m Adaptive Paper Training
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/btc-5m-flow-paper.yml'
permissions:
  contents: read
jobs:
  paper:
    runs-on: ubuntu-latest
    timeout-minutes: 355
    steps:
      - uses: actions/checkout@v4
      - name: Validate PAPER_ONLY
        run: |
          python -m py_compile experiments/polymarket-paper/btc_5m_flow_surveillance_paper.py
          ! grep -q 'PRIVATE_KEY' experiments/polymarket-paper/btc_5m_flow_surveillance_paper.py
          ! grep -q 'POST /order' experiments/polymarket-paper/btc_5m_flow_surveillance_paper.py
          grep -q '"real_orders":0' experiments/polymarket-paper/btc_5m_flow_surveillance_paper.py
      - name: Run 50 adaptive BTC windows
        run: python experiments/polymarket-paper/btc_5m_flow_surveillance_paper.py
      - name: Package
        if: always()
        run: |
          python - <<'PY'
          import pathlib,zipfile
          p=pathlib.Path('btc_5m_training_runs48_97')
          with zipfile.ZipFile('btc-5m-training-results-50.zip','w',zipfile.ZIP_DEFLATED) as z:
              for f in p.glob('*'):
                  if f.is_file(): z.write(f,f.name)
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: btc-5m-training-results-50
          path: btc-5m-training-results-50.zip
          retention-days: 7
          if-no-files-found: error
